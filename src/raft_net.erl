%% @doc Networking behaviour.
-module(raft_net).

%%% Callbacks ==================================================================
-callback foo(_) -> ok | error.

%%% Exports ====================================================================
-export([ start_link/1 ]).

%%% API ========================================================================
start_link(_Args) ->
  ignore.

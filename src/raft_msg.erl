%% @doc Messaging behaviour.
-module(raft_msg).

%%% Callbacks ==================================================================
-callback foo(_) -> ok | error.

%%% Exports ====================================================================
-export([ start_link/1 ]).

%%% API ========================================================================
start_link(_Args) ->
  ignore.
